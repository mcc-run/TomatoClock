package com.example.tomatoclock.TaskSet

import com.example.tomatoclock.TaskList.Task

data class set(var id : Int = -1,var name : String = "",var taskset: MutableList<Task> = mutableListOf<Task>()) {

//    待办集合名

//    任务集合
}